export const DETECTION_TYPES=["motion","person","vehicle","animal","object","line_crossing","zone_intrusion","occupancy","tamper","smoke","fire","unknown"] as const;
export type DetectionType=typeof DETECTION_TYPES[number];
export function normalizeDetectionType(v:any):DetectionType{const s=String(v??"").trim().toLowerCase().replace(/[\s-]+/g,"_");if(/human/.test(s))return"person";if(/car|truck|motor|bike/.test(s))return"vehicle";if(/movement|suspicious_motion/.test(s))return"motion";if(/intrusion/.test(s))return"zone_intrusion";if(/line/.test(s))return"line_crossing";if(/tamper|obstruct/.test(s))return"tamper";return(DETECTION_TYPES as readonly string[]).includes(s)?s as DetectionType:"unknown"}
export function normalizeBoundingBox(raw:any){const b=raw&&typeof raw==="object"&&!Array.isArray(raw)?raw:Array.isArray(raw)&&raw.length===4?{x:raw[0],y:raw[1],width:raw[2],height:raw[3]}:{};const values=[b.x,b.y,b.width??b.w,b.height??b.h].map(Number);if(values.some(v=>!Number.isFinite(v)))return null;const[x,y,width,height]=values;if(x<0||y<0||width<0||height<0||x>1||y>1||x+width>1||y+height>1)return null;return{x,y,width,height}}
