import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const files=["media.ts","detection.ts"];const roots=[path.join(root,"../facility-oyi/lib/oyi-camera-core"),path.join(root,"../Oyi-os-frontend/src/lib/oyi-camera-core")];for(const targetRoot of roots)for(const file of files){fs.mkdirSync(targetRoot,{recursive:true});fs.copyFileSync(path.join(root,"packages/oyi-camera-core/src",file),path.join(targetRoot,file));}console.log(`Synced Camera Core media/detection contracts to ${roots.length} surfaces`);
