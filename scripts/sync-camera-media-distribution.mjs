import fs from "node:fs";import path from "node:path";import {fileURLToPath} from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const source=path.join(root,"packages/oyi-camera-core/src/media.ts");const targets=[process.env.FACILITY_CAMERA_MEDIA_PATH||path.join(root,"../facility-oyi/lib/oyi-camera-core/media.ts"),process.env.CONSUMER_CAMERA_MEDIA_PATH||path.join(root,"../Oyi-os-frontend/src/lib/oyi-camera-core/media.ts")];for(const target of targets){fs.mkdirSync(path.dirname(target),{recursive:true});fs.copyFileSync(source,target);}console.log(`Synced Camera Core media contract to ${targets.length} surfaces`);
