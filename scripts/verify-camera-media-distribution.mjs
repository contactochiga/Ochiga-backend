import fs from "node:fs";import path from "node:path";import assert from "node:assert/strict";import {fileURLToPath} from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const canonical=fs.readFileSync(path.join(root,"packages/oyi-camera-core/src/media.ts"),"utf8");const targets=[process.env.FACILITY_CAMERA_MEDIA_PATH||path.join(root,"../facility-oyi/lib/oyi-camera-core/media.ts"),process.env.CONSUMER_CAMERA_MEDIA_PATH||path.join(root,"../Oyi-os-frontend/src/lib/oyi-camera-core/media.ts")];for(const target of targets){assert(fs.existsSync(target),`Camera Core media distribution missing: ${target}`);assert.equal(fs.readFileSync(target,"utf8"),canonical,`Camera Core media drift: ${target}`);}console.log("Camera Core Phase 4 media distribution verified");
