import fs from "node:fs";import path from "node:path";import assert from "node:assert/strict";import {fileURLToPath} from "node:url";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");for(const file of ["media.ts","detection.ts"]){const canonical=fs.readFileSync(path.join(root,"packages/oyi-camera-core/src",file),"utf8");for(const targetRoot of [path.join(root,"../facility-oyi/lib/oyi-camera-core"),path.join(root,"../Oyi-os-frontend/src/lib/oyi-camera-core")]){const target=path.join(targetRoot,file);assert(fs.existsSync(target),`Camera Core distribution missing: ${target}`);assert.equal(fs.readFileSync(target,"utf8"),canonical,`Camera Core drift: ${target}`)}}console.log("Camera Core Phase 5 media/detection distribution verified");
